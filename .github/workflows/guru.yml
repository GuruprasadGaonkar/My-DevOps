name: Devops_perfect

on:
    workflow_dispatch: 

jobs:

    Linux:
        runs-on: ubuntu-latest
        steps:
            - name: basic command
              run: echo "i am perfect in basic commands"
